name = "minimal"
description = "Minimal template — git identity + SSH key"

[dotfiles]
".gitconfig" = { source = "inline", content = """
[user]
    name = Agent
    email = agent@localhost
[safe]
    directory = *
""" }
".ssh/id_ed25519" = { source = "generate" }
