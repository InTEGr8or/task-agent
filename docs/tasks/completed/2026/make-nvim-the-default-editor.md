# Make NVim the default editor

Fall back to vim, if NVim is not available.

* Allow this to be configured.
* Create a standard config that uses NVim as the default and falls back to Vim. 
* Allow the users to change it.

---
**Completed in commit:** `2459b65`
