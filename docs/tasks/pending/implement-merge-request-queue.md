# Implement merge-request queue

Create a 'merge-request/' (or 'mr/') subdirectory in 'docs/tasks/'. This folder will serve as a 'return' queue where agentic workers can post completion datagrams (JSON or Markdown) for human or supervisor review.
