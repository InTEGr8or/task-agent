# Create a Plan feature

Create a ``Plan.md`` in the ``tasks/`` to contain the long-term strategy direction that the app is being developed in. Create a ``ta plan`` command to edit the file in the selected editor. Render the nicely-formatted plan before the "Available Commands", when the user executes ``ta`` with no command.

---
**Completed in commit:** `<pending-commit-id>`
